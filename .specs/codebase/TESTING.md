# Testing Infrastructure

## Test Frameworks

**Unit/Integration:** nenhum configurado
**E2E:** nenhum configurado
**Coverage:** n/a

## Test Organization

Não existe. O projeto não possui `package.json`, runner de testes ou pipeline de CI.

## Testing Patterns

Nenhum automatizado. Validação hoje é manual: abrir `index.html` no navegador e conferir visualmente.

## Test Execution

Não aplicável.

## Coverage Targets

**Current:** 0%
**Goals:** não definidos — dado o tamanho do projeto (site estático de vitrine), o plano de verificação recomendado é QA manual guiado por checklist (ver `tasks.md` de cada feature, seção "Done when") em vez de suíte automatizada, a menos que o projeto cresça para incluir lógica de negócio mais complexa (ex.: carrinho, autenticação).
**Enforcement:** nenhuma
